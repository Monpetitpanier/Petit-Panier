import React from 'react';
import { PlaceholderScreen } from '../_PlaceholderScreen';
import { colors } from '@/theme/colors';

export function BourseScreen() {
  return (
    <PlaceholderScreen
      titre="La Bourse"
      sousTitre="De la visibilité, jamais de la culpabilité. (à construire : issue #6)"
      couleur={colors.bourse}
    />
  );
}
